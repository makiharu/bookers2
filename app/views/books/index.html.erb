
<div class="container">
  <div clsss="row">
    <!--use info -->
    <%= render 'layouts/userinfo', user: @user %>


    <!--book一覧-->
    <div class="col-xs-9">
      <h1>Books</h1>
      <table class="table">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
          </tr>
        </thead>
        <tbody>
          <% @books.each do |book| %>
          <tr>
            <td></td>　　　<!--修正-->
            <th><%= book.title %></th>
            <td><%= book.body %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>


  　　<!--新規投稿-->
  　　<%= render 'layouts/newbook', book: @book %>


</div>